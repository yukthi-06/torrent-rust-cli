```text
Build a production-quality cross-platform BitTorrent client in Rust.

# Goal

Create a modern BitTorrent client consisting of two binaries:

1. torrentd
   - Long-running background daemon
   - Owns all torrent state
   - Handles networking, disk I/O, scheduling, and persistence

2. torrent
   - Foreground CLI client
   - Connects to the daemon over local IPC
   - Never directly manipulates torrent data
   - Simply sends commands and prints results

The project should be structured as a Cargo workspace and written with clean, modular, idiomatic Rust.

--------------------------------------------------
Requirements
--------------------------------------------------

Target platforms:

- Windows x64
- Windows ARM64
- Linux x64
- Linux ARM64

Must compile on stable Rust.

Use async Rust with Tokio.

--------------------------------------------------
Workspace Layout
--------------------------------------------------

workspace/

    crates/
        core/
        daemon/
        cli/
        rpc/
        storage/
        tracker/
        dht/
        peer/
        piece/
        config/

--------------------------------------------------
Architecture
--------------------------------------------------

torrentd owns everything.

CLI communicates only through IPC.

Daemon responsibilities:

- torrent management
- peer management
- tracker communication
- DHT
- piece selection
- piece verification
- SHA1 hashing
- disk IO
- bandwidth limiting
- resume support
- persistence
- logging
- configuration
- RPC server

CLI responsibilities:

torrent create <file/folder>

torrent add <torrent>

torrent remove <id>

torrent pause <id>

torrent resume <id>

torrent list

torrent status

torrent stats

torrent info <id>

torrent verify <id>

torrent config

torrent version

--------------------------------------------------
IPC
--------------------------------------------------

Use platform-native IPC.

Linux:
Unix Domain Socket

Windows:
Named Pipe

Abstract transport behind a trait so almost all code is shared.

Implement a binary protocol instead of HTTP.

Packet format:

Version
Command
Length
Payload

Serialize payloads using bincode.

Support request/response.

Support graceful reconnect.

--------------------------------------------------
Storage
--------------------------------------------------

Use file-based persistent storage instead of SQLite.

Store all application state under a configurable data directory.

Example layout:

data/
    config.toml
    torrents/
        <torrent-id>.json
    resume/
        <torrent-id>.bin
    metadata/
        <infohash>.torrent
    logs/
    downloads/

Requirements:

- Use serde for serialization.
- Store metadata as JSON for readability.
- Store resume/piece state as compact binary using bincode.
- Write files atomically (write to a temporary file, then rename).
- Never corrupt state on unexpected shutdown.
- Automatically create missing directories.
- Support loading all torrents on daemon startup.
- Persist state incrementally rather than rewriting everything.
- Allow future schema versioning.
- Implement backward-compatible migrations where practical.

--------------------------------------------------
Configuration
--------------------------------------------------

Use TOML.

Example:

download_dir

listen_port

max_upload

max_download

max_connections

enable_dht

enable_utp

enable_upnp

--------------------------------------------------
Torrent Features
--------------------------------------------------

Implement:

✓ Magnet links

✓ .torrent files

✓ Bencode parsing

✓ SHA1 piece verification

✓ Multi-file torrents

✓ Single-file torrents

✓ Tracker announce

✓ UDP tracker

✓ HTTP tracker

✓ Peer wire protocol

✓ Choking

✓ Interested

✓ Bitfield

✓ Have

✓ Request

✓ Piece

✓ Cancel

✓ Fast resume

✓ Sequential download option

✓ Piece picker

✓ Rarest-first algorithm

✓ End-game mode

--------------------------------------------------
Networking
--------------------------------------------------

Async networking using Tokio.

Efficient handling of hundreds of peers.

No blocking operations.

Graceful shutdown.

Connection timeouts.

Retry logic.

Bandwidth throttling.

--------------------------------------------------
Logging
--------------------------------------------------

Use tracing.

Support

ERROR

WARN

INFO

DEBUG

TRACE

--------------------------------------------------
CLI
--------------------------------------------------

Use clap.

Support:

torrent add ubuntu.torrent

torrent add magnet:...

torrent list

torrent status

torrent stats

torrent pause 3

torrent resume 3

torrent remove 3

torrent verify 3

Output should be clean and human readable.

--------------------------------------------------
Code Quality
--------------------------------------------------

Use:

tokio
serde
serde_json
bincode
clap
tracing
thiserror
anyhow
sha1
bitvec
toml

Avoid unnecessary external dependencies.



Follow idiomatic Rust.

Avoid unwrap() in production code.

Use Result throughout.

Use traits where appropriate.

Separate protocol from implementation.

Write modular code.

Document public APIs.

--------------------------------------------------
Testing
--------------------------------------------------

Include:

unit tests

integration tests

protocol tests

parser tests

piece verification tests

IPC tests

--------------------------------------------------
GitHub Actions
--------------------------------------------------

Create CI that builds release binaries for:

Linux x64

Linux ARM64

Windows x64

Windows ARM64

Upload artifacts.

Create GitHub Release on version tags.

--------------------------------------------------
Documentation
--------------------------------------------------

Generate:

README.md

Architecture documentation

Developer guide

IPC protocol documentation

Configuration documentation

--------------------------------------------------
Development Strategy
--------------------------------------------------

Develop incrementally in small, reviewable milestones.

Suggested order:

1. Workspace setup
2. Common crates
3. IPC layer
4. SQLite storage
5. Daemon lifecycle
6. CLI
7. Torrent parser
8. Peer protocol
9. Tracker support
10. Download engine
11. Piece picker
12. Resume support
13. DHT
14. Magnet links
15. Bandwidth limiting
16. Testing
17. GitHub Actions
18. Documentation

At each milestone:
- Ensure the code compiles.
- Add tests.
- Avoid placeholder implementations unless explicitly marked as TODO.
- Keep commits small and logically organized.
- Prioritize maintainability, correctness, and cross-platform compatibility over premature optimization.
```
