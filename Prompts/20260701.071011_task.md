# User Prompt
> once a create-add is done, the file should be served immediately from the location of the file/folder.  But app shows it is downloading....

# Execution Task List for `create-add` In-Place Seeding

- `[x]` Define `SavedTorrent` struct in `server.rs`.
- `[x]` Refactor `load_state` and `save_state` to use `SavedTorrent` and support fallback migration.
- `[x]` Update `saved_paths` type in `RpcServer` to `HashMap<u32, SavedTorrent>`.
- `[x]` Add `custom_download_dir: Option<PathBuf>` to `add_torrent_internal`.
- `[x]` Update `restore_torrent` to check `saved_paths` for custom download dir.
- `[x]` Update `Request::Add` and `Request::CreateAdd` dispatch logic.
- `[x]` Update `Request::Resume`, `Request::Verify`, etc. to accommodate `SavedTorrent`.
