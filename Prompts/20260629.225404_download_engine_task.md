# User Prompt
```text
add is successful, but no downloading is happenng
```

# BitTorrent Client In Rust - Download Engine Task List

- [ ] Implement Peer Wire Protocol parsing and message structures (handshake, choke/unchoke, request, piece, etc.)
- [ ] Implement UDP Tracker announce protocol in `crates/tracker`
- [ ] Implement TCP connection loop and P2P handshakes in `crates/peer`
- [ ] Create the core download task and file writer in `crates/daemon/src/engine.rs`
- [ ] Integrate download engine with `Request::Add` so downloading starts on torrent addition
- [ ] Format and verify all files compile successfully
