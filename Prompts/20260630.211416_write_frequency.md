# User Prompt
> when daemon is killed, a lot of downloaded data is lost.  Make the writes more frequest, to minimize such data loss

# Implementation
- Changed the `tokio::time::interval` in `crates/daemon/src/main.rs` from 30 seconds to 5 seconds to reduce data loss on unexpected shutdown.
