# User Prompt
> Failed: Could not fetch metadata from any of

# Analysis
Still failing to fetch metadata from all peers. Since we can't see the daemon logs easily, need to surface per-peer error details in the status message so the user can report them. The root cause is likely one of:
1. Peers not supporting ut_metadata (leechers without metadata)
2. Extended handshake parsing issues
3. Timeout issues

# Fix
- Surface per-peer error details in the failure status message
- Add retry logic with delays between attempts
