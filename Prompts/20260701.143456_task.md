# User Prompt
> proceed with implementation of seeding

# Execution Task List

- `[x]` Update `crates/daemon/src/server.rs` to add `Arc<TorrentDownloader>` to `TorrentHandle`.
- `[x]` Update `TorrentDownloader::new` to create a `broadcast::Sender<u32>`.
- `[x]` Refactor `TorrentDownloader::handle_peer` to use `into_split()`.
- `[x]` Implement bidrectional logic in `handle_peer` to send `Bitfield`, `Unchoke`, and answer `Request` with `Piece` reads.
- `[x]` Implement TCP listener on port 6881 in `server.rs` to accept incoming handshakes and route to `TorrentDownloader::handle_peer`.
- `[x]` Track uploaded metrics in `TorrentState::uploaded`.
