# User Prompt
> a list of announce tracker urls is stored in a txt file trackers.txt. Use this list to create the torrent

# Execution Task List

- `[x]` Update `crates/rpc/src/lib.rs` to include `trackers: Option<Vec<String>>` in `Create` and `CreateAdd` requests.
- `[x]` Update `crates/daemon/src/creator.rs` to take a list of trackers and populate both `announce` and `announce-list`.
- `[x]` Update `crates/cli/src/main.rs` to read `trackers.txt` relative to the CLI's working directory.
- `[x]` Update `crates/daemon/src/server.rs` to extract the trackers list from the RPC request.
