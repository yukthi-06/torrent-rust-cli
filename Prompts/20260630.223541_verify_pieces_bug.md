# User Prompt
> what was once a completed torrent(100%), after restart it is 56%  and sometimes 44%, and so on... multiple torrents are showing like this

# Implementation
- Identified bug in `crates/daemon/src/engine.rs` where padding files (all zeros) were being incorrectly rejected during hash verification.
- Removed the `piece_data.iter().all(|&b| b == 0)` check which incorrectly caused valid padding pieces to be marked as missing.
